---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'h1'>

const props = Astro.props
---

<h1
  {...props}
  class:list={[
    'm-0 text-[clamp(2rem,5vw,2.65rem)] font-bold tracking-wide',
    props.class,
  ]}
>
  <slot />
</h1>
